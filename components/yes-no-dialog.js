import styles from "styles/yes-no-dialog.module.scss";

// props contains title and message
const YesNoDialog= function (props) {
    return (
        <div className={styles.divDialogWall}>
            
        </div>
    )
};

export default YesNoDialog;